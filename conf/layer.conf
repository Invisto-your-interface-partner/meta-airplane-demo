# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-airplane-demo"
BBFILE_PATTERN_meta-airplane-demo = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-airplane-demo = "6"

LAYERDEPENDS_meta-airplane-demo = "core"
LAYERSERIES_COMPAT_meta-airplane-demo = "kirkstone"
